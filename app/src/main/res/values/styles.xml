<?xml version="1.0" encoding="utf-8"?>

<resources
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    >

    <!-- Base application theme. -->
    <style name="Theme.Speech" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:color">@color/white</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="android:actionBarTabTextStyle">@style/Theme.AppCompat</item>
    </style>

    <style name="product_detail_cardview">

        <item name="android:background">@color/white</item>
    </style>
    <style name="Theme.Speech.AppBarOverlay" parent="ThemeOverlay.AppCompat" />

    <style name="Theme.Speech.PopupOverlay" parent="ThemeOverlay.AppCompat" />

</resources>
